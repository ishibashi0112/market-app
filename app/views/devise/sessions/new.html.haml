= render 'items/header_sub'
.userMain
  .userForm
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .userForm__header
        %h1
          会員情報入力
      .userForm__group
        .userForm__group--label
          = f.label :email,'メールアドレス', class: 'label'
          %span.indispensable
            必須
        = f.email_field :email,placeholder: "PC・携帯どちらでも可", autofocus: true, class: 'userForm__group--input'
      .userForm__group
        .userForm__group--label
          = f.label :password,'パスワード', class: 'label'
          %span.indispensable
            必須
        = f.password_field :password, autocomplete: "off", class: 'userForm__group--input'
      .userForm__btn
        = f.submit "ログイン", class: 'userForm__btn--red'
      .userForm__btn
        .userForm__btn--facebook
          = link_to '        Facebookでログイン', user_facebook_omniauth_authorize_path, method: :post, class: 'fab fa-facebook-square'
      .userForm__btn
        .userForm__btn--google
          = link_to '        Googleでログイン', user_google_oauth2_omniauth_authorize_path, method: :post, class: 'fab fa-google'
= render 'items/footer_sub'